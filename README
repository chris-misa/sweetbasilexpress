New work can be done locally on the newfeatures branch.

To make changes live, run

local $ git push

server $ git merge newfeatures


To reflect server-side changes back into local working branch:

local $ git checkout master
      $ git pull
      $ git checkout newfeatures
      $ git merge master
